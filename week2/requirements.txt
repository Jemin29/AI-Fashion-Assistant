# =============================================================================
# Week 2 — Text-to-Image Fashion Generation Foundation
# Requirements: Stable Diffusion XL + Supporting Libraries
# =============================================================================

# ── Core Diffusion / Generation ───────────────────────────────────────────────
diffusers>=0.27.0                # Hugging Face Diffusers (SDXL support)
transformers>=4.38.0             # CLIP, text encoders
accelerate>=0.28.0               # Distributed inference, CPU offload
safetensors>=0.4.2               # Fast, safe model weight loading
xformers>=0.0.25                 # Flash-attention memory optimization (optional)
omegaconf>=2.3.0                 # Structured config (DictConfig)

# ── Image Processing ──────────────────────────────────────────────────────────
Pillow>=10.2.0                   # Core image I/O and manipulation
opencv-python-headless>=4.9.0    # OpenCV (no GUI) for image transforms
torchvision>=0.17.0              # Vision utilities & transforms
einops>=0.7.0                    # Tensor reshaping utilities

# ── ML / Numerical ────────────────────────────────────────────────────────────
torch>=2.2.0                     # PyTorch (CPU/CUDA/MPS)
numpy>=1.26.0                    # Numerical computing
scipy>=1.12.0                    # Scientific computing (SSIM etc.)

# ── Evaluation & Metrics ─────────────────────────────────────────────────────
open-clip-torch>=2.24.0          # OpenCLIP for CLIP similarity scoring
torchmetrics>=1.3.0              # Structured metrics (FID, SSIM, PSNR)
lpips>=0.1.4                     # Learned Perceptual Image Patch Similarity
pytorch-fid>=0.3.0               # FID computation (InceptionV3 activations)

# ── Configuration & Validation ───────────────────────────────────────────────
pydantic>=2.6.0                  # Typed config models & validation
pydantic-settings>=2.2.0         # Settings from env / .env files
pyyaml>=6.0.1                    # YAML config file parsing

# ── Logging ───────────────────────────────────────────────────────────────────
loguru>=0.7.2                    # Structured, colourful logging
rich>=13.7.0                     # Rich console output & progress bars
tqdm>=4.66.0                     # Lightweight progress bars

# ── Storage & Utilities ───────────────────────────────────────────────────────
huggingface-hub>=0.21.0          # Model download & cache management
requests>=2.31.0                 # HTTP for remote asset fetching
python-dotenv>=1.0.0             # Load .env files

# ── Type Checking & Dev ───────────────────────────────────────────────────────
typing-extensions>=4.10.0        # Extra typing utilities

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-cov>=5.0.0
pytest-mock>=3.12.0

# ── Notebooks ─────────────────────────────────────────────────────────────────
jupyter>=1.0.0
ipywidgets>=8.1.0
matplotlib>=3.8.0

# ── Optional: ONNX Runtime (for optimised CPU inference) ─────────────────────
# onnxruntime>=1.17.0
# onnx>=1.15.0
